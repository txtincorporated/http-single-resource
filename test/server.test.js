const assert = require('assert');
const server = require('../lib/server');

//Test server, router and request handlers
describe('server preps and passes request data to router', () => {
  it('passes test 1', () => {
  });
});

describe('router preps, passes route-specific server-side and req data, and function calls to handlers', () => {
  it('passes test 2', () => {
  });
});

describe('handlers process data correctly and complete server response to client', () => {
  it('passes test 3', () => {
  });
});